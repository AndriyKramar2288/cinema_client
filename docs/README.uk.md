# Undisputed Canvas

Веб-додаток для бронювання місць у кінотеатрі з інтеграцією Google OAuth та адміністративною панеллю управління.

[🇺🇸 English](../README.md) | [🇺🇦 Українська](README.uk.md)

---

![picture 1](docs_resources/images/picture%201.png)
![picture 2](docs_resources/images/picture%202.png)
![picture 3](docs_resources/images/picture%203.png)
![picture 4](docs_resources/images/picture%204.png)

## Опис проєкту

Undisputed Canvas — сучасний веб-застосунок кінотеатру, що пропонує інтуїтивний інтерфейс для вибору сеансів та бронювання місць у залі задля деякого перегляду. Система включає розширену адміністративну панель для управління фільмами, сеансами та залами.

## Основні можливості

### Для відвідувачів

- **Перегляд афіші**: детальна інформація про фільми, що йдуть у прокаті
- **Бронювання квитків**: покроковий процес резервування місць
- **Візуалізація зали**: інтерактивна схема розміщення у кінозалі
- **Автентифікація через Google**: швидкий вхід за допомогою облікового запису Google
- **Особистий кабінет**: перегляд історії бронювань

### Для адміністраторів

- **Управління фільмами**: додавання, редагування та видалення фільмів
- **Парсинг даних**: автоматичне отримання інформації про фільми з UAkino
- **Управління сеансами**: створення розкладу показів
- **Управління залами**: налаштування кінозалів та їх характеристик
- **Моніторинг бронювань**: перегляд всіх бронювань із можливістю скасування

### Для працівників

- **Перегляд актуальних бронювань**: доступ до інформації про заброньовані місця на майбутні сеанси

## Технологічний стек

### Frontend

- **Next.js 15.2.5** — React-фреймворк для серверного рендерингу
- **React 19** — бібліотека для побудови інтерфейсів
- **Tailwind CSS 4** — utility-first CSS фреймворк
- **Framer Motion 12** — бібліотека для анімацій
- **React Toastify** — система сповіщень
- **Google OAuth** — автентифікація користувачів
- **FontAwesome** — набір іконок

### Інфраструктура

- **Docker** — контейнеризація додатку
- **Node.js 20.17.0** — серверне середовище

## Структура проєкту

```
cinema_client/
├── src/
│   ├── app/
│   │   ├── booking/[film_id]/    # Сторінка бронювання
│   │   ├── profile/               # Особистий кабінет
│   │   ├── page.js                # Головна сторінка
│   │   ├── layout.js              # Основний layout
│   │   └── globals.css            # Глобальні стилі
│   └── components/
│       ├── booking_page/          # Компоненти сторінки бронювання
│       ├── common/                # Спільні компоненти
│       ├── contexts/              # React Context
│       ├── main_page/             # Компоненти головної сторінки
│       ├── profile_page/          # Компоненти профілю
│       └── services/              # Допоміжні функції та сервіси
├── public/                        # Статичні ресурси
├── Dockerfile                     # Конфігурація Docker
├── package.json                   # Залежності проєкту
└── tailwind.config.js            # Конфігурація Tailwind
```

## Встановлення та запуск

### Передумови

- Node.js версії 18.18.0 або вище
- npm або yarn
- Docker (опціонально)

### Локальна розробка

1. Клонуйте репозиторій:
```bash
git clone https://github.com/AndriyKramar2288/cinema_client
cd cinema_client
```

2. Встановіть залежності:
```bash
npm install
```

3. Створіть файл `.env.local` у корені проєкту:
```env
API_URL=http://localhost:8080/api
```

4. Запустіть сервер розробки:
```bash
npm run dev
```

5. Відкрийте браузер за адресою [http://localhost:3000](http://localhost:3000)

### Збірка для продакшену

```bash
npm run build
npm start
```

### Збірка та запуск Docker Compose

У разі використання docker compose, виконайте наступні дії:

#### 1. Підготовка проекту

Клонуйте обидва репозиторії в одну директорію:

```bash
# Створіть робочу директорію
mkdir cinema-app
cd cinema-app

# Клонуйте backend
git clone https://github.com/AndriyKramar2288/cinema_server

# Клонуйте frontend
git clone https://github.com/AndriyKramar2288/cinema_client

# Створіть директорію для секретів БД
mkdir db
```

#### 2. Налаштування бази даних

Створіть файл `db/password.txt` з паролем для PostgreSQL:

```bash
echo "your_secure_database_password" > db/password.txt
```

**Важливо**: Переконайтеся, що `db/password.txt` додано до `.gitignore`!

#### 3. Створення конфігурації

Створіть файл `compose.yaml` у кореневій директорії:

```yaml
services:
  server:
    build:
      context: ./cinema_server
    ports:
      - 8080:8080
    secrets:
      - db-password
    depends_on:
      db:
        condition: service_healthy

  client:
    build:
      context: ./cinema_client
    environment:
      - NODE_ENV=production
      - REACT_APP_API_URL=http://localhost:8080
    ports:
      - 3000:3000
    depends_on:
      - server

  db:
    image: postgres:16-alpine
    restart: always
    user: postgres
    secrets:
      - db-password
    volumes:
      - cinema_volume_prod:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=cinema_db
      - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
    expose:
      - 5432
    healthcheck:
      test: ["CMD", "pg_isready"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  cinema_volume_prod:

secrets:
  db-password:
    file: db/password.txt
```

#### 4. Запуск

```bash
# Збірка та запуск усіх сервісів
docker compose up -d --build

# Перегляд логів
docker compose logs -f

# Перегляд логів конкретного сервісу
docker compose logs -f server
docker compose logs -f client
docker compose logs -f db
```

#### 5. Перевірка

Після запуску перевірте доступність сервісів:
- Frontend: http://localhost:3000
- Backend API: http://localhost:8080

#### 6. Управління

```bash
# Зупинка сервісів
docker compose down

# Зупинка з видаленням volumes (УВАГА: видалить БД!)
docker compose down -v

# Перезапуск конкретного сервісу
docker compose restart server

# Перегляд статусу
docker compose ps

# Підключення до БД
docker compose exec db psql -U postgres -d cinema_db
```

## Налаштування Google OAuth

1. Створіть проєкт у [Google Cloud Console](https://console.cloud.google.com)
2. Увімкніть Google+ API
3. Створіть OAuth 2.0 Client ID
4. Додайте авторизовані origins: `http://localhost:3000`
5. Замініть Client ID у файлі `src/components/services/nonComponents.js`

## Backend API

Додаток взаємодіє з backend API за адресою, вказаною у змінній `API_URL`. Основні ендпоінти:

- `GET /api/films/with_sessions` — отримання фільмів із доступними сеансами
- `GET /api/session/available/by_film/{id}` — отримання сеансів для фільму
- `POST /api/booking/` — створення бронювання
- `POST /api/auth/google_id_token` — автентифікація через Google
- `GET /api/users/session/` — отримання бронювань користувача

Детальніша документація API має надаватися backend-командою.

## Особливості реалізації

### Система бронювання

Процес бронювання складається з трьох етапів:

1. **Вибір сеансу**: користувач обирає дату та час показу
2. **Вибір місць**: інтерактивна схема зали з візуалізацією вільних та зайнятих місць
3. **Введення даних**: заповнення контактної інформації для кожного місця

### Адміністративна панель

Панель управління включає три основні розділи:
- **Фільми**: CRUD-операції з можливістю парсингу даних
- **Сеанси**: створення розкладу показів
- **Зали**: управління кінозалами

### Система ролей

Додаток підтримує три рівні доступу:
- **Гість**: перегляд фільмів та бронювання
- **Користувач**: додатково особистий кабінет
- **Працівник**: перегляд всіх бронювань
- **Адміністратор**: повний доступ до управління системою

## Стилізація

Проєкт використовує:
- **Tailwind CSS** для utility-класів
- **Custom fonts** від Google Fonts (PT Mono, Dancing Script, Cormorant Garamond та інші)
- **Градієнти та backdrop filters** для ефекту глибини
- **Responsive design** з підтримкою мобільних пристроїв